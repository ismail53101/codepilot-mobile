<?xml version="1.0" encoding="utf-8"?>
<!--
  CodePilot network security config.

  Android 9+ blocks cleartext HTTP by default. CodePilot's Live Preview
  serves project files over a loopback HTTP server (http://127.0.0.1) so
  the WebView can resolve relative CSS/JS/assets; without this config the
  release build fails with net::ERR_CLEARTEXT_NOT_PERMITTED.

  Scope is intentionally narrow: cleartext is allowed ONLY to loopback
  (127.0.0.1 / localhost). All other traffic must be HTTPS.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
